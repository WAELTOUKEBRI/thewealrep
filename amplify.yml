version: 1
frontend:
  phases:
    preBuild:
      commands:
        - gem install bundler
        - bundle install
    build:
      commands:
        - jekyll build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'

